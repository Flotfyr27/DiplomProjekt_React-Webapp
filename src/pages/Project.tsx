import { FC } from "react";
import Helmet from "react-helmet";

const Project: FC = () => {
  return (
    <div>
      <Helmet>
        <title>Projekter</title>
      </Helmet>
      <h1>This where we list our projects!</h1>
    </div>
  );
};
export default Project;
